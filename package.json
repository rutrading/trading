{
  "name": "trading",
  "private": true,
  "scripts": {
    "setup": "bun run scripts/setup.ts",
    "dev": "bun run --cwd web dev",
    "migrate": "bun run --cwd web --env-file .env.local bunx @better-auth/cli migrate"
  }
}
