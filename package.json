{
  "name": "trading",
  "private": true,
  "scripts": {
    "setup": "bun run backend/scripts/setup.ts",
    "dev": "concurrently --kill-others --timestamp-format HH:mm:ss.SSS --prefix \"{time} [{name}]\" -n web,api,md,tf,ps -c cyan,red,blue,green,yellow \"bun run --cwd web dev\" \"uv run --directory backend/api uvicorn app.main:app --reload\" \"uv run --directory backend/services/market_data python -m app.server\" \"uv run --directory backend/services/transformer python -m app.server\" \"uv run --directory backend/services/persistence python -m app.server\"",
    "dev:gen": "uv run --directory backend python scripts/gen_proto.py",
    "migrate": "bun run --cwd web bunx drizzle-kit push"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
